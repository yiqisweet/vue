import Vue from 'vue'import Vuex from 'vuex'import VueRouter from 'vue-router'import VueResource from 'vue-resource'var App = require('./App.vue');var state = require('./vuex/state');var mutations = require('./vuex/mutations');var getters = require('./vuex/getters');var routes = require('./router.config');Vue.use(Vuex);  //注入到所有子组件Vue.use(VueRouter);Vue.use(VueResource);const store = new Vuex.Store({ //创建store的实例，进行store模块的整合    state : state,    mutations : mutations,    getters : getters})//创建路由实例，放路由整合const router = new VueRouter({    routes : routes})new Vue({    el : '#app',    store, //注入    router, //注入    render : function(h){        "use strict";        return h(App)    }})